# PANISA task homepage

The user's GitHub account is `moeionblog-create`. This repository was transferred from `juliantkw1-tech` on 17 September 2026 at the user's request. The canonical repository is `moeionblog-create/panisa-home` and Pages origin is `https://moeionblog-create.github.io/panisa-home/`. Verify the active authenticated account before future publication. Use the owner's signed-in browser if the CLI is authenticated as another account. Never create a replacement under Julian's account.

Public GitHub Pages homepage and sanitized report. On 17 September 2026 the user explicitly requested migrating the report to their GitHub account without login. Use report.html for the portable report and relative links from the homepage. Publish only reviewed, sanitized report data: anonymize customer names and remove contact details, order/account identifiers, private conversation text and internal audit paths. Never upload raw exports or tokens. Preserve stable anonymous ad keys for historical comparisons.

Use tasks.json as the source of task status. Read README.md's status contract before updates. Never infer a completed run from its schedule, a page visit or successful publication of this website. Customer-list use for advertising requires verified rights; data originally supplied through Shopee is excluded until those rights are confirmed. Do not treat chat-order phones as automatically authorized either.

Check status logic, rendered desktop/mobile layout, filtering, report links and failed-fetch behavior when relevant. Publish through the user's GitHub Pages. The old report service is not the canonical menu destination.

Update 17 September 2026: The user superseded the no-login preference and requested Google login with original customer names behind authentication. Keep the task homepage public and sanitized. report.html is a redirect to https://panisa-private-report.moeionblog.workers.dev/report.html on the owner's Cloudflare account; preserve tab/date query parameters. NEVER upload the private report or customer data to this GitHub repository. No data refresh is implied by the login change.
